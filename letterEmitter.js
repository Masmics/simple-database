const EventEmitter = require('events');
const ee = new LetterEmitter();

class LetterEmitter.extends(EventEmitter);
  read(str) {          // a default constructor!!
    //split the string
    //forEach letter in string emit a letter event
    //pass along some data like { letter: 'e', offset: 1 }
    //after reaching the end emit an end event
  }
  //     this.letter = 'b';
  //     this.offset = 20;
  // }
}

const letters = () => {
  let str = ee.read.stringify;
  let array = str.split(',');
  
}; 

array.forEach(iterate(array[i])) {

  // beforeEach(done => {
  //   const ee = new EventEmitter();
  // })

